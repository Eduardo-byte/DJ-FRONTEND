// Re-export axios instance from the new API structure for backward compatibility
export { default } from '../api/config/axiosInstance';
export { clearSessionCache } from '../api/config/axiosInstance';
